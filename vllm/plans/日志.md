以下是该特性**全部新增的日志打印**，按文件和执行路径分类：

---

### vLLM 侧

#### 1. `vllm/v1/engine/core.py` — Store 路径（release_kv_cache）
| 级别 | 日志内容 | 触发时机 |
|------|---------|---------|
| `INFO` | `"Offloaded %d transfer batches to CPU"` | GPU→CPU 拷贝完成后 |

#### 2. `vllm/v1/engine/core.py` — Load 路径（add_request 恢复）
| 级别 | 日志内容 | 触发时机 |
|------|---------|---------|
| `INFO` | `"CPU cache hit: %d blocks (%d tokens) for request %s"` | 在 CPU 缓存中找到前缀命中 |
| `WARNING` | `"Not enough GPU blocks to restore %d blocks from CPU"` | GPU block 不足无法恢复 |
| `INFO` | `"Restored %d blocks from CPU to GPU for request %s"` | CPU→GPU 恢复完成 |

#### 3. `vllm/v1/worker/gpu_worker.py` — GPU Worker
| 级别 | 日志内容 | 触发时机 |
|------|---------|---------|
| `INFO` | `"Allocated %d CPU tensors with %d blocks each for release KV cache offloading"` | 初始化 CPU pinned memory 完成 |
| `WARNING` | `"Release CPU caches not initialized, skipping offload"` | 未配置 `num_release_cpu_blocks` 时调用了 offload |
| `WARNING` | `"Release CPU caches not initialized, skipping load"` | 未配置 `num_release_cpu_blocks` 时调用了 load |

---

### vllm-ascend 侧

#### 4. `vllm_ascend/worker/worker.py` — NPU Worker
| 级别 | 日志内容 | 触发时机 |
|------|---------|---------|
| `INFO` | `"Allocated %d CPU cache tuples with %d blocks each for release KV cache offloading"` | 初始化 CPU pinned memory 完成 |
| `WARNING` | `"Release CPU caches not initialized, skipping offload"` | 未配置时调用了 offload |
| `WARNING` | `"Release CPU caches not initialized, skipping load"` | 未配置时调用了 load |

---

共 **9 条**新增日志。关键的可观测日志是：
- 启动时看到 `INFO "Allocated ..."` 说明 CPU 内存分配成功
- 运行时看到 `DEBUG "CPU cache hit ..."` 和 `"Restored ..."` 说明恢复路径生效
- 如果看到 `WARNING "Not enough GPU blocks ..."` 说明 GPU 内存紧张，无法恢复