Commit `57c45626b` created on branch `feature/kv-cache-sharing-plugin-merge`. Here's a summary of all changes:

**New files (2):**
- `vllm/v1/core/two_phase_block_queue.py` - Doubly-linked list queue with aging zone for priority eviction
- `vllm/v1/core/kv_cache_session_manager.py` - Per-session block reference counting

**Modified files (14):**

| Layer | File | Changes |
|-------|------|---------|
| Protocol | `vllm/engine/protocol.py` | Added `release_kv_cache()` to `EngineClient` |
| Block Pool | `vllm/v1/core/block_pool.py` | Replaced `FreeKVCacheBlockQueue` with `TwoPhaseBlockQueue`, added `aging_block()` |
| Single Type Manager | `vllm/v1/core/single_type_kv_cache_manager.py` | Added `aging_block()`, `save_new_computed_blocks_with_session()`, `allocate_new_blocks_with_session()`, session manager init |
| Coordinator | `vllm/v1/core/kv_cache_coordinator.py` | Added `aging_block()`, session-aware allocation/save methods |
| KV Cache Manager | `vllm/v1/core/kv_cache_manager.py` | Added `release_kv_cache()`, switched `allocate_slots` to use session-aware methods |
| Scheduler Interface | `vllm/v1/core/sched/interface.py` | Added `release_kv_cache()` |
| Scheduler | `vllm/v1/core/sched/scheduler.py` | Implemented `release_kv_cache()` |
| Request | `vllm/v1/request.py` | `from_engine_core_request()` now extracts `sharing_cache_salt` |
| Engine Core | `vllm/v1/engine/core.py` | Added `release_kv_cache()`, `pack/unpack_sharing_cache_salt()`, `encode/decode_engine_core_request()` |
| Async LLM | `vllm/v1/engine/async_llm.py` | Added `release_kv_cache()` |
| Core Client | `vllm/v1/engine/core_client.py` | Added `release_kv_cache()` to `EngineCoreClient` and `AsyncMPClient` |
| OpenAI Protocol | `vllm/entrypoints/openai/protocol.py` | Added `cache_sharing` field to `ChatCompletionRequest`, new `ReleaseKvCacheRequest`/`ReleaseKvCacheResponse` |
| API Server | `vllm/entrypoints/openai/api_server.py` | Added `POST /release_kv_cache` route |
| Serving Chat | `vllm/entrypoints/openai/serving_chat.py` | Modified `create_chat_completion()` for cache sharing, added `release_kv_cache()` method |